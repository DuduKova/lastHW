import { OnProductHoverDirective } from './on-product-hover.directive';

describe('OnProductHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new OnProductHoverDirective();
    expect(directive).toBeTruthy();
  });
});
